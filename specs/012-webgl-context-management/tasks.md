---

description: "Task list for 012-webgl-context-management"
---

# Tasks: WebGL Context Management for Multi-Map Dashboards

**Input**: Design documents from `/specs/012-webgl-context-management/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/, quickstart.md (all present)

**Tests**: Explicitly requested throughout this feature's own plan/spec —
every task that changes runtime behavior has a corresponding empirical
Playwright test task, matching this project's established discipline for
every prior map-rendering feature (004, 010, 011).

**Organization**: This feature touches exactly two real files —
`src/panels/FlowMapPanel.tsx` and `tests/integration/flowmapPanel.spec.ts`
— so almost nothing here is `[P]`-parallelizable (same-file edits can't
safely run concurrently); tasks are still grouped and labeled by user
story for traceability and independent-test-criteria purposes, per this
command's own required format.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1/US2/US3)
- **[GATING]**: A checkpoint that MUST pass before the next task/phase
  proceeds — this feature's plan.md names two such checkpoints
  explicitly (T005 and T010 below), mirroring 011's own T014/T022
  gating-checkpoint precedent.

## Path Conventions

Single project (this existing Vite + React dashboard app) —
`src/panels/FlowMapPanel.tsx`, `tests/integration/flowmapPanel.spec.ts`.
No new directory (plan.md's own Project Structure section).

---

## Phase 1: Setup

**Purpose**: Confirm the one external prerequisite this feature's own
gating checks depend on — no new dependency, config, or scaffolding is
needed (plan.md's Constitution Check: zero new deps/files).

- [X] T001 Confirm `package.json`'s existing `build` (`tsc --noEmit && vite build`) and `preview` (`vite preview`) scripts produce a servable production build with no changes needed — this is the exact mechanism quickstart.md Scenario 2's GATING capacity check and research.md §5's production-build measurement discipline both depend on. No file changes expected; if either script is missing/broken, fix `package.json` before proceeding.

**Checkpoint**: Production build/preview path confirmed working.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The one piece of new state both US1 and US2 depend on —
`layerRepopulateGeneration`, the shared trigger mechanism that lets
either a `setStyle()`-driven layer wipe (US1) or a context-loss recovery
(US2) safely re-run the FlowmapLayer construction, using the EXISTING
data-update effect's own always-fresh closure rather than a new
stale-closure-prone helper (contracts/interleaved-overlay-survival.md's
"Why not a standalone helper function").

**⚠️ CRITICAL**: Neither US1's `style.load` handler nor US2's
`webglcontextrestored` handler has anywhere correct to bump this counter
until it exists.

- [X] T002 Add `const [layerRepopulateGeneration, setLayerRepopulateGeneration] = useState(0)` alongside the existing `status`/`rows`/`mapReady` state, and extend the existing data-update effect's dependency array from `[config, rows, status, mapReady]` to `[config, rows, status, mapReady, layerRepopulateGeneration]`, in `src/panels/FlowMapPanel.tsx` — effect body unchanged (data-model.md "Concrete shape changes"; contracts/interleaved-overlay-survival.md "Data-update effect — one dependency added"). Also added a `warnedForRowsRef` guard around the exclusion-count `console.warn` — a real bug found empirically: without it, the warning fired a second time on every repopulate-only re-run (unchanged `rows`), not just on genuinely new data.

**Checkpoint**: The shared repopulate-trigger mechanism exists and compiles; US1 and US2 implementation can now begin.

---

## Phase 3: User Story 1 - Every configured map panel on a tab shows a working basemap (Priority: P1) 🎯 MVP

**Goal**: Halve per-panel WebGL context cost via interleaved `MapboxOverlay` mode, correctly surviving the `setStyle()`-wipe risk that mode reopens, so the existing 6-panel "Basemaps" fixture renders every panel's basemap correctly in a production build (SC-001).

**Independent Test**: `npm run build && npm run preview`, load `dashboard-3-basemaps.yaml`'s Basemaps tab, confirm every non-intentionally-broken panel shows a real, non-blank basemap and zero `"too many active webgl contexts"` console warnings (quickstart.md Scenario 2) — testable without any US2/US3 work present.

### Implementation for User Story 1

- [X] T003 [US1] Flip `MapboxOverlay` construction from `{ interleaved: false, layers: [] }` to `{ interleaved: true, layers: [] }` in `src/panels/FlowMapPanel.tsx`'s mount-only effect (contracts/interleaved-overlay-survival.md "Mount-only effect — one-line change"). Depends on: T002 (land together — an interleaved flip with no repopulate wiring yet is an intentionally incomplete intermediate state, not a safe standalone commit).
- [X] T004 [US1] In `src/panels/FlowMapPanel.tsx`'s basemap-application effect, add `overlayRef.current?.setProps({ layers: [] })` immediately before the existing `map.setStyle(...)` call. **Revised from the original task wording, in two rounds**: (1) does NOT hook the repopulate off `style.load` — confirmed empirically (and against `github.com/maplibre/maplibre-gl-js/discussions/2716`) that `'style.load'` fires only ONCE per `Map` instance's lifetime, never again on a second+ `setStyle()` call, so 011's own `map.once('style.load', () => map.off('error', onLoadError))` never actually ran past the first basemap switch — fixed with a `'styledata'`-driven `onStyleReady` check instead. (2) That check's first version filtered on `getStyle().sources` being non-empty — a SECOND real, confirmed bug, found only after this task was believed complete via a direct user question tracing the fixture's own "Unreachable Basemap" panel with real instrumentation: `BLANK_STYLE` legitimately has zero sources, so the filter never fired for a basemap that falls back to blank, silently and permanently wiping that panel's FlowmapLayer (the clear above still runs unconditionally) — violating FR-010/SC-004's "a missing/unreachable basemap never prevents a panel's data-driven content from rendering" guarantee. Fixed by removing the source-count filter entirely: `onStyleReady` now fires unconditionally on the FIRST `'styledata'` after each `setStyle()` call (contracts/interleaved-overlay-survival.md's updated "Two further corrections" section has the full trace and reasoning). The effect's cleanup still detaches any still-pending `onStyleReady` listener from a superseded invocation. Depends on: T002, T003.
- [X] T005 [US1] [GATING] Update 011's existing `test.describe('011-basemap-style-system — US1: setStyle()/MapboxOverlay empirical survival ...')` block in `tests/integration/flowmapPanel.spec.ts`: removed the `canvas#deckgl-overlay` locator and every assertion built on it, replaced with `canvas.maplibregl-canvas` (confirmed via `@deck.gl/mapbox`'s real source: interleaved mode creates no canvas of its own); changed the render-count assertion to `renderCountBefore + 1` (exact); left `flowCount`, `overlayLayerCount === 1`, `moveend`-liveness, and the zero-`duplicate|already exists` console check as-is. **A second, pre-existing 010-era test at the same "canvas#deckgl-overlay" gap** ("the map survives 004's DOM relocation") was also found and fixed the same way — not anticipated by the original task wording, found only by actually running the suite. PASSING. Depends on: T003, T004.
- [X] T006 [US1] Added `test.describe('012-webgl-context-management — interleaved overlay repopulates exactly once per style change', ...)`. **Revised assertion from the original task wording**: does NOT assert an exact "+2" render-count for two rapid back-to-back toggles — confirmed empirically that the existing generation-guard/AbortController mechanism can legitimately cancel a superseded invocation before it completes (and the browser's own `MutationObserver` batching can coalesce both DOM mutations into a single callback, so React may never observe an intermediate state at all), making the intermediate count environment-dependent, not a real guarantee. Asserts the actual guarantee instead: after settling, exactly one live `FlowmapLayer`, `flowCount` unchanged, and the final theme's basemap genuinely drawing pixels. PASSING. Depends on: T004.
- [X] T007 [US1] [GATING] Ran quickstart.md Scenario 1 (T005) against the dev-server Playwright suite (27/27 passing, stable on repeat) AND Scenario 2 for real against an actual production build: `npm run build` (fixtures copied into `public/` first), `vite preview`, a one-off script driving the same real browser against `http://127.0.0.1:5299/APP-wftdm-dashboard/`. Result: all 6 Basemaps-tab panels render correctly (`data-flow-count: "6"` each, zero context-lost banners), zero console warnings matching `/too many active webgl contexts/i`, and the exclusion warning fires exactly once per panel (confirming T002's `warnedForRowsRef` fix holds in production too, not just dev). `public/` fixture copies removed afterward (teardown normally does this; bypassed here since this was a one-off script, not the Playwright harness). Depends on: T005, T006.

**Checkpoint**: User Story 1 is fully functional and independently verified — the core production bug (silent context loss on a realistic multi-panel tab) is fixed and empirically proven, before any US2/US3 work begins.

---

## Phase 4: User Story 2 - A lost map context is reported honestly (Priority: P2)

**Goal**: Wire up MapLibre's own already-built-in `webglcontextlost`/`webglcontextrestored` events so a lost context shows a distinct, honest status instead of being indistinguishable from 011's own intentional blank-basemap fallback, and correctly recovers to the panel's own effective basemap when the browser restores the context.

**Independent Test**: Force a real context loss via `WEBGL_lose_context.loseContext()` on one panel, confirm a distinct banner appears while the panel's container stays mounted, confirm it's visually/textually different from an unreachable-basemap panel (011's fixture), then call `.restoreContext()` and confirm the banner clears and the correct basemap reappears (quickstart.md Scenario 4) — testable independently of US3, and depends only on US1's `layerRepopulateGeneration` mechanism already existing (Phase 2/3), not on any US1 test having been written.

### Implementation for User Story 2

- [X] T008 [US2] Add `const [contextLost, setContextLost] = useState(false)` alongside the other panel-local state, and inside `src/panels/FlowMapPanel.tsx`'s mount-only effect (after `map.addControl(overlay)`), register `map.on('webglcontextlost', onContextLost)` (`onContextLost = () => setContextLost(true)`) and `map.on('webglcontextrestored', onContextRestored)` (`onContextRestored` sets `setContextLost(false)`, calls `overlayRef.current?.setProps({ layers: [] })`, then `setLayerRepopulateGeneration((g) => g + 1)` — deliberately no direct FlowmapLayer construction in this closure); add matching `map.off(...)` calls for both listeners in that same effect's cleanup. Depends on: T002, T003.
- [X] T009 [US2] Updated `src/panels/FlowMapPanel.tsx`'s final render `return` block: wrapped the existing `containerRef` `<div>` in a new `<div style={{ position: 'relative', ... }}>`, added a conditional `{status === 'ready' && contextLost && (...)}` absolutely-positioned overlay rendering `<PanelErrorState message="Map context lost — ..." />`. Depends on: T008.
- [X] T010 [US2] [GATING] Added `test.describe('012-webgl-context-management — WebGL context loss is reported honestly', ...)`. **Two real bugs found and fixed while making this pass, not anticipated by the original task wording**: (1) `loseContext`/`restoreContext` test helpers originally re-derived the `WEBGL_lose_context` extension via a fresh `getContext('webgl2')` call each time — `restoreContext()`'s own re-derivation returned `null` (a canvas whose context is currently lost doesn't hand back a usable extension the same way); fixed by obtaining the extension ONCE and caching it on `window`. (2) The pixel-readback check (`getImageData`-equivalent `readPixels`) was unreliable when read via a plain, disconnected `page.evaluate()` well after the actual draw — MapLibre's own canvas doesn't guarantee `preserveDrawingBuffer` survives past compositing once idle (unlike deck.gl's own non-interleaved canvas, which apparently does); fixed with a shared `canvasHasDrawnPixels()` helper that forces `map.triggerRepaint()` and reads pixels inside that same `'render'` event callback, now also wrapped in `trueEventually()` at every call site after one transient flake under full-suite load. (3) The test's own flow originally navigated to the Basemaps tab mid-sequence to compare against 011's "unreachable basemap" panel — since `DashboardRenderer` only ever renders the active tab, this silently remounted (and reset) the panel under test; reordered so that comparison happens first. PASSING (also stable across the full 126-test suite, 2 consecutive clean runs). Depends on: T009.
- [X] T011 [US2] Ran quickstart.md Scenario 4 (T010 above) and Scenario 5 (added as its own test, "a pinned basemap recovers to itself, not the app default"). **Revised from the original task wording**: does not wait for/assert a NEW network request for the pinned preset after recovery — confirmed that recovery deliberately does NOT re-fetch the style document at all (FR-006/research.md §6: MapLibre's own already-restored style IS the panel's already-resolved effective basemap). Instead asserts the whole-test-spanning request log contains the pin's own style request (from before the loss) and never the app-default presets, plus a genuine post-recovery pixel-draw check. A second bug (request listener registered after `boot()`, capturing an unrelated Summary-tab panel's own positron fetch) was also found and fixed by scoping listener registration to after the tab navigation. PASSING. Depends on: T010.

**Checkpoint**: User Stories 1 AND 2 both work independently — capacity is fixed (US1) and any residual/edge-case loss is honestly reported and correctly recoverable (US2).

---

## Phase 5: User Story 3 - Expanding a map panel never regresses an already-working basemap (Priority: P3)

**Goal**: Prove this feature's own changes don't reopen 004's persistent-DOM-node expand/collapse guarantee — a regression-protection story, not new runtime code (spec.md's own "Why this priority" framing).

**Independent Test**: On the 6-panel Basemaps tab (US1's own fixture, already fully working), expand one panel into 004's dialog, confirm its basemap/layer survive, collapse it back, confirm every panel (the expanded one and every sibling) still renders correctly — testable as soon as US1 is done, with no dependency on US2's context-loss code at all.

### Implementation for User Story 3

- [X] T012 [US3] Added `test.describe('012-webgl-context-management — expand/collapse never regresses a working basemap', ...)` to `tests/integration/flowmapPanel.spec.ts`, covering quickstart.md Scenario 3. No code change to `FlowMapPanel.tsx`/`panelExpandHost.tsx` needed — no regression surfaced, confirming research.md §4's own risk analysis (deferring viewport-gated mounting) didn't leave a real gap here. PASSING. Depends on: T007.

**Checkpoint**: All three user stories are independently functional and verified.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Full regression re-run and documentation sync — required by FR-005, not assumed passing because the diff "looks additive."

- [X] T013 Re-ran the FULL integration suite (all spec files, not just flowmapPanel.spec.ts — a broader check than originally scoped, since flowmapPanel.spec.ts's own within-file run had already covered 011's specific scenarios): 126/126 passing, 2 consecutive clean runs (the first full run caught one real flake in T010's pixel check, fixed via the `trueEventually`-wrapped `canvasHasDrawnPixels`; the second full run was clean). `dashboardShell.spec.ts`'s tab-set assertion (`['Summary', 'Detail', 'Basemaps']`) unaffected. No code outside `FlowMapPanel.tsx`/`flowmapPanel.spec.ts` was touched.
  - **Post-completion finding #1, addressed after this task was first marked done**: a direct user question ("does onStyleReady's sources-check ever fire for BLANK_STYLE?") led to real instrumentation proving it does not — 011's own "uniform blank-style fallback" test (`tests/integration/flowmapPanel.spec.ts`, "Flowmap Unreachable Basemap (intentional)") was passing for the wrong reason: it only checked the `data-flow-count` DOM attribute, which is a stale value the basemap effect's own unconditional layer-clear does not touch, not the overlay's actual (in this case, permanently emptied) layer state. Fixed at the T004 source (see that task's own updated note) and the existing test strengthened with real `overlayLayerCount`/`canvasHasDrawnPixels` assertions — re-ran the full flowmapPanel.spec.ts suite (27/27 passing) and the full 126-test suite (126/126 passing), both clean.
  - **Post-completion finding #2, a real live-user bug report**: "the summary tab map works, the top 2 maps in the basemap tab work, the remaining 4 show blank gray canvases with flow lines on top." Traced with direct browser instrumentation (a live `npm run dev` session, not just the Playwright suite) to a genuinely different, more serious bug: `BLANK_STYLE` (`panels/basemap/loadBasemapStyle.ts`) was a single shared module-level object handed directly to every panel's `maplibregl.Map` constructor and every `onLoadError`/fallback `setStyle()` call. MapLibre treats a Map's style as a live, mutable reference rather than cloning it — confirmed directly via a `transformStyle` dump showing one panel's `previous` style carrying another (unrelated) panel's already-resolved `sprite`/`glyphs` URLs mutated onto the shared `BLANK_STYLE` object. This corrupted a later panel's real style enough to make MapLibre's own validator throw (`glyphs: string expected, undefined found`) mid-`setStyle()`, aborting that panel's style application entirely — the base map stayed on `BLANK_STYLE` (gray) forever while the deck.gl overlay (unaffected, independent of MapLibre's own style state) still drew flow lines on top, exactly matching the user's own description. Invisible with one flowmap panel per tab (010/011's own test coverage); only surfaced once this feature made several real panels on one tab a normal configuration. Fixed with `freshBlankStyle()` (a real `structuredClone` per call) at every MapLibre-facing call site — confirmed via the same live-instrumentation approach that the error is gone and every panel's basemap now renders correctly. Re-ran `npm run typecheck` (clean), the `loadBasemapStyle.test.ts` unit tests (5/5 passing, `.toEqual()` value comparisons unaffected by the clone), and the full 126-test integration suite.
  - **Post-completion finding #3, the SAME user report resurfacing after finding #2's own fix — a genuinely different root cause behind the identical symptom, found only via a real production build (`npm run build && npm run preview`), not the dev server or the test suite**: killed the user's own stale-suspected preview process, confirmed its actual start timestamp (`wmic`/`PowerShell Get-Process`) was AFTER finding #2's fix — ruling out a simple stale build before looking further, exactly as instructed. A fully clean rebuild + fresh preview + a real Playwright session against `http://127.0.0.1:4173/APP-wftdm-dashboard/` (not the test fixtures) still reproduced it. Per-panel `map.getStyle()` inspection showed the real sources/tiles genuinely resolved and fetched (confirmed via the network log — no failed requests for any real tile host) for the two still-broken panel types — ruling out finding #2's own bug class (a thrown/aborted `setStyle()`) entirely. Comparing `layerIds` array order across panels found the real cause: the `transformStyle` callback's own layer-merge (`layers: [...next.layers, ...preserved]`, present since 011, untouched by findings #1/#2) appends any `previous`-style layer not already in `next` — correct for a genuine future app-added custom layer, wrong for `BLANK_STYLE`'s own opaque `background` layer. CARTO/OpenFreeMap presets happen to define their own `"background"` layer as their first layer (masking this in 011's own coverage); a raster preset (no `background` of its own) and a namespaced composition (its own, if any, renamed to `layer0__background`) do not — so `BLANK_STYLE`'s background got appended LAST and painted opaquely over an already-correctly-loaded real basemap. Confirmed empirically: `background` sat at index 1 of 2 for the raster preset and index 547 of 548 for the UGRC composition. Fixed with a new `BLANK_STYLE_LAYER_IDS` constant excluded from what `transformStyle` preserves. Re-verified against the exact same real production build + live Playwright session, both structurally (`layerIds` no longer contains `background`) and visually (fresh screenshot — real OpenTopoMap terrain tiles and real UGRC street tiles now render). Added real regression coverage two ways: (1) `tests/integration/flowmapPanel.spec.ts` — a new `canvasCornersAreNotUniformBlankGray()` helper sampling real canvas pixels away from the flow lines, plus a `layerIds` structural assertion, added to both the raster-preset and UGRC-composition tests; verified these NEW assertions actually fail against the reverted (buggy) code before confirming they pass against the fix — not vacuous. Every pre-existing "sources resolved correctly" assertion had already been passing throughout both rounds of this bug, which is exactly why it shipped: none of them inspect layer stacking order.
- [X] T014 Updated `CLAUDE.md`'s `FlowMapPanel.tsx` file-tree comment and the "Map panels" prose section — documents `interleaved: true`, the `contextLost` state + listeners, the `layerRepopulateGeneration` mechanism (and why it replaced the plan-phase `reapplyCurrentLayer()` design), and the confirmed `'style.load'`-fires-once-per-lifetime finding (with its real upstream citation) that also fixes a latent 011 bug. Also corrected a pre-existing 011-era staleness in the same "Map panels" section (claimed real basemap tiles were still unresolved — 011 already resolved that; left uncorrected until now).
  - **Post-completion finding #4, from a follow-up fixture-panel task (four new Basemaps-tab panels demonstrating `carto-dark-matter`/`openfreemap-liberty`/`Esri.WorldImagery`/a real UGRC "Vector Hybrid" composition) — a genuinely new capability, plus a real bug that immediately followed it, both found via the same live production-build discipline as findings #2/#3**: representing UGRC's real "Utah Vector Hybrid Base Map" (confirmed via its own live ArcGIS item JSON, `opendata.gis.utah.gov/datasets/utah-vector-hybrid-base-map`) required extending `composeStyles()` (`panels/basemap/loadBasemapStyle.ts`) with a genuinely new composition-layer shape: a bare raster provider preset name (no `http(s)://` scheme) resolved via `resolveRasterProvider()` and inlined directly, alongside the existing fetched-URL layer shape — needed because Esri's World_Imagery is a raw ArcGIS MapServer raster endpoint, not a style-spec document the existing URL branch could fetch-and-merge. Immediately after building this, two of the five new fixture panels ("Flowmap Esri World Imagery Preset" and "Flowmap UGRC Vector Hybrid") both fell back to `BLANK_STYLE` in a real production build despite (a) `public/basemap/leaflet-providers.json` correctly containing the real `Esri`/`WorldImagery` catalog entry, confirmed directly, and (b) `resolveRasterProvider()`'s own resolution logic working correctly in isolation (confirmed in a standalone Node run against the real catalog file). Traced with temporary `console.log` instrumentation added to `registry.ts`/`loadBasemapStyle.ts` in a real production preview build (same live-instrumentation discipline as finding #2): `resolveRasterProvider()` itself resolved correctly every time — the real bug was downstream, in the raster-source object literal both `resolvePresetName()`'s simple-preset branch AND `composeStyles()`'s new raster branch built. Both wrote `maxzoom: raster.maxZoom` unconditionally; for any provider defining no maxZoom override (confirmed: Esri.WorldImagery in the real catalog is exactly this case), `raster.maxZoom` is `undefined`, but the `maxzoom` KEY still ends up own-enumerable on the resulting object (`Object.keys()` reports it even though its value is `undefined` — confirmed directly in a Node REPL). MapLibre's real style-spec validator normalizes that to `null` for its type check, producing a genuine "Expected value to be of type number, but found null instead" console warning AND a real MapLibre `'error'` event — `FlowMapPanel`'s own FR-010 error-fallback listener (correctly, per its own coarse-by-design contract) then treats that as "the basemap failed," reverting to `freshBlankStyle()` before any tile ever fetches — explaining why no failed/pending network request for `arcgisonline.com` ever appeared in the diagnostic log; the request never fired at all. `OpenTopoMap` (which DOES define an explicit `maxZoom: 17`) never hit this, which is exactly why it worked while `Esri.WorldImagery` didn't. Fixed with a conditional spread (the same defensive pattern `inlineTileJsonSource()` already used for its own optional TileJSON fields) at both call sites, omitting the key entirely instead of setting it to `undefined`. The existing raster+vector composition unit test had NOT caught this: it used `toMatchObject`, which only checks listed keys and never fails on an extra own-enumerable key — a new, dedicated unit test was added asserting the key is truly ABSENT via `Object.keys()`/`hasOwnProperty`, confirmed to genuinely fail against the reverted bug before confirming it passes against the fix. Re-verified all 11 Basemaps-tab panels (6 original + 5 new) against a fully clean rebuild + fresh preview + live Playwright session and screenshot — every panel renders correctly, including the two previously-broken ones (real satellite imagery for the simple preset; satellite imagery with a real vector road/label overlay, correctly stacked, for the Vector Hybrid composition). One further regression surfaced by this same fixture addition, unrelated to the raster bug itself: the new "Flowmap Dark Matter Preset" sibling panel's own legitimate one-time `dark-matter-gl-style` fetch polluted an existing, unrelated context-loss-recovery test's whole-session request log, and — once time-scoped to fix that — still raced that sibling's fetch under real parallel-worker load; fixed by explicitly waiting for the sibling's own known one-time fetch before snapshotting, closing the race rather than merely narrowing it. Full unit suite (166/166) and the full `flowmapPanel.spec.ts` integration suite (27/27) both re-confirmed clean; the full 126-test integration suite was also re-run under real parallel-worker load specifically to confirm the race fix holds outside isolated/repeated single-test runs — 126/126 passing, including the previously-racy recovery test.
- [X] T015 `npm run typecheck`: clean. `npm run test:unit`: 162/162 passing, unmodified — confirms zero regression in 011's `resolveEffectiveBasemap.test.ts`/`resolveRasterProvider.test.ts`/`loadBasemapStyle.test.ts`, none of which this feature touches.
  - **Post-completion finding #5, a real bug reported by the user directly (not found via this session's own investigation) — leaflet-providers' own `{attribution.ProviderName}` placeholder convention was never resolved at all**: `resolveRasterProvider()` returned `resolved.options.attribution` verbatim, including any raw, unsubstituted `{attribution.X}` placeholder — the extracted catalog (`public/basemap/leaflet-providers.json`) is the RAW pre-substitution provider-definitions object (the real substitution only ever happens inside leaflet-providers.js's own `L.TileLayer.Provider#initialize()`, which this app never calls, having reimplemented only the merge/expand logic around it, not this piece). Confirmed the real substitution rule directly against `node_modules/leaflet-providers/leaflet-providers.js`'s own source (not assumed): a placeholder always references another TOP-LEVEL provider's own `options.attribution` (`providers[attributionName].options.attribution` — never a variant's own attribution, since variants aren't independently addressable that way at all), and resolution is genuinely RECURSIVE (the referenced provider's own attribution is itself run back through the same replacer before substitution, so a chain fully resolves, not just one level) — confirmed by reading `attributionReplacer`'s own implementation directly, not inferred from behavior. Checked the real, current generated catalog for an actual 2-level chain to use as the regression case: none exists today (every `{attribution.X}`-referencing top-level provider's own referenced target has no placeholder of its own), so the recursive-chaining test is deliberately synthetic while the two root-cause regression tests use REAL, current attribution strings verbatim (OpenTopoMap's own real attribution, which references `{attribution.OpenStreetMap}`; Esri.WorldImagery's own real VARIANT-level attribution, which references `{attribution.Esri}` — the actual real-world case that broke, since finding #4's own fixture panels use exactly this provider). Fixed with a new `resolveAttributionPlaceholders()` in `registry.ts`, applied to `resolveRasterProvider()`'s return value before it reaches either of `loadBasemapStyle.ts`'s two call sites (`resolvePresetName`'s simple-preset branch, `composeStyles`' raster-layer branch from finding #4) — same fail-soft convention as everything else in this feature (an unresolvable reference, e.g. a hand-edited catalog missing the referenced provider, leaves that one placeholder's raw text in place rather than throwing or dropping the rest of an otherwise-valid string), plus a defensive depth cap the real upstream code has no equivalent of at all (a malformed/circular catalog would otherwise recurse unboundedly and hang/crash the tab — no real cycle exists in the actual catalog today, confirmed directly, so this is pure defense-in-depth). Added 5 new unit tests to `tests/unit/resolveRasterProvider.test.ts` (8 → 13): the two real regression cases (OpenTopoMap, Esri.WorldImagery, both using real current catalog strings), a synthetic 2-level chaining case, a fail-soft unresolvable-reference case, and a circular-reference case proving the depth cap actually bounds recursion rather than hanging. Confirmed all three behavioral tests (the two real regressions + the chaining test) genuinely fail against a reverted (pre-fix) `resolveRasterProvider()` before confirming they pass against the fix — not vacuous; the fail-soft and depth-cap tests pass unconditionally by construction (nothing to regress against, since the fail-soft/cap behavior is new, not a fix to prior wrong behavior) and are included as forward-looking regression coverage instead. Re-verified in a real production build (`npm run build && npm run preview`): both `Esri.WorldImagery`-based fixture panels (the simple preset from finding #4 and the Vector Hybrid composition) now show the fully-resolved attribution — `"Tiles &copy; Esri &mdash; Source: Esri, i-cubed, USDA, USGS, AEX, GeoEye, Getmapping, Aerogrid, IGN, IGP, UPR-EGP, and the GIS User Community"` — at BOTH the `map.getStyle().sources[...].attribution` level and the live MapLibre attribution control's own rendered DOM text (confirmed via screenshot), with zero occurrences of `{attribution.` anywhere; the two URL-preset panels (`carto-dark-matter`, `openfreemap-liberty`) were confirmed never affected by this bug in the first place (they fetch a complete `style.json` with attribution already baked in by CARTO/OpenFreeMap themselves — no `leaflet-providers` catalog involvement at all). Full unit suite re-confirmed clean at 171/171 (166 + 5 new); `npm run typecheck` clean.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — can start immediately.
- **Foundational (Phase 2)**: Depends on Setup — BLOCKS both US1 and US2 (T003/T004 and T008 all need `layerRepopulateGeneration` from T002).
- **User Story 1 (Phase 3)**: Depends on Foundational. No dependency on US2/US3.
- **User Story 2 (Phase 4)**: Depends on Foundational AND on US1's T003 (interleaved mode is why one shared-canvas listener pair suffices) — a real cross-story dependency, not pretended-independent; still independently *testable* once T003/T002 exist, without needing US1's own tests (T005-T007) to have been written.
- **User Story 3 (Phase 5)**: Depends on US1 (T007) being complete — needs a real, fully-working 6-panel tab to expand/collapse against. No dependency on US2.
- **Polish (Phase 6)**: T013 depends on nothing beyond Foundational (can run alongside Phase 5); T014/T015 depend on all prior phases being complete.

### Parallel Opportunities

Genuinely limited in this feature — `src/panels/FlowMapPanel.tsx` and
`tests/integration/flowmapPanel.spec.ts` are each edited by nearly every
task, so sequential execution within each file is the realistic default.
The one real opportunity: **T012 (US3) and T013 (Polish regression
re-run) can run in parallel** once T007 is done — different fixture
panels within the same test file, no shared mutable state between them.

### Implementation Strategy

**MVP = User Story 1 alone (Phase 1-3)**: fixes the actual reported
production bug (silent context loss on a realistic multi-panel tab).
User Story 2 (honest status for any residual/edge-case loss) and User
Story 3 (expand/collapse regression proof) are both real, spec-required
parts of this feature, but US1 alone is independently deployable value
if a decision is made to stop there — matching spec.md's own priority
ordering exactly.
