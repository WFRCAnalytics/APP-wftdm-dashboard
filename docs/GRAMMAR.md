# WFRC TDM Calibration Dashboard — YAML Grammar Specification

**Version:** 0.2  
**Stack:** DuckDB (Python + WASM) · Parquet · GeoParquet · Vite · Plotly · Observable Plot · MapLibre · flowmap.gl · Graphic Walker

---

## Overview

The dashboard is driven entirely by three YAML file types, all **authored**
alongside the model scripts in the TDM repo — not in this repository. Each has
its own authored-vs-published split; see "File layout" below for exactly
where each one's published copy is fetched from.

| File | Who writes it | What it does | Published to this repo at |
|---|---|---|---|
| `summarize.yaml` | Modeler (once per model version) | Post-processor config: sources, mappings, bins, SQL fragments, metrics → Parquet | never — post-processor-only, not read by the browser |
| `dashboard-*.yaml` | Analyst | Tab layout and panel definitions. First file = landing page. | `public/dashboard-config/` |
| `manifest.yaml` | Post-processor (auto-generated) | Per-scenario metadata: name, date, color | `public/scenarios/{name}/` (per scenario) |

**What does not exist:**
- No `topsheet.yaml` — `dashboard-1-summary.yaml` is the landing page
- No `summarize-preprocessor.yaml` — join logic lives in `sql_fragments`
- No `dashboard-config.yaml` — app settings live in code and CLI arguments

---

## File layout

**TDM repo (model side):**
```
Scenarios/
  _default/                   ← existing scenario folder
    manifest.yaml             ← auto-generated by post-processor
    summary/                  ← written by post-processor
      summary_kpis.parquet    ← first file loaded; powers landing page value boxes
      trip_mode_share.parquet
      tour_mode_share.parquet
      tlfd.parquet
      trip_scheduling.parquet
      auto_ownership.parquet
      telecommute_freq.parquet
      cdap.parquet
      mandatory_tour_freq.parquet
      non_mandatory_tour_freq.parquet
      stop_frequency.parquet
      screenlines.parquet
      od_flows.parquet
      workplace_od_flows.parquet
      vmt_by_home_taz.parquet
      # ... one file per metric defined in summarize.yaml

  2027-RTP-BaseYear/          ← another scenario
    manifest.yaml
    summary/
      ...
```

**Dashboard repo (web app side):**
```
public/
  observed/                   ← always pre-loaded, deselectable
    manifest.yaml             ← pinned: true, name: "Observed Data"
    summary/
      summary_kpis.parquet
      observed_mode_share.parquet
      observed_counts.parquet
      observed_tlfd.parquet
      observed_trip_rates.parquet

  scenarios/                  ← published model runs (auto-discovered at startup)
    index.json                ← ["2027-rtp-baseyear", "2027-rtp-horizonyear"]
    2027-rtp-baseyear/        ← copied from TDM repo Scenarios/2027-RTP-BaseYear/
      manifest.yaml
      summary/
        summary_kpis.parquet
        ...
    2027-rtp-horizonyear/
      manifest.yaml
      summary/
        ...

  dashboard-config/           ← published tab layout, discovered + fetched at startup
    index.json                ← ["dashboard-1-summary.yaml", "dashboard-2-person.yaml", ...]
    dashboard-1-summary.yaml
    dashboard-2-person.yaml
    dashboard-3-tour.yaml
    dashboard-4-mode.yaml
    dashboard-5-trip.yaml
    dashboard-6-network.yaml
    dashboard-7-explore.yaml   ← WFRC's default set; index.json can list any set
```

**Publish workflow:** the TDM repo and dashboard repo are separate project folders.
To publish a scenario, use any file manager (FileZilla, Windows Explorer, etc.):
1. Copy `Scenarios/{run-name}/summary/` → `APP-wftdm-dashboard/public/scenarios/{name}/summary/`
2. Copy `Scenarios/{run-name}/manifest.yaml` → `APP-wftdm-dashboard/public/scenarios/{name}/manifest.yaml`
3. Add `"{name}"` to `public/scenarios/index.json` in the dashboard repo
4. Commit and push the dashboard repo → GitHub Actions deploys automatically

To publish dashboard layout changes (not per-scenario — published once, shared by
every scenario): copy the `dashboard-*.yaml` files from wherever they're authored in
the TDM repo → `APP-wftdm-dashboard/public/dashboard-config/`, update
`public/dashboard-config/index.json` to list exactly the filenames now present (same
step as `public/scenarios/index.json`, just for tabs), commit, push.

**Fetch mechanism:** `summarize.yaml` is authored alongside model scripts in the TDM
repo and is post-processor-only — it is never published and the browser never reads
it. `dashboard-*.yaml` is also authored there, but its published copy **is** fetched
by the browser at startup: `main.ts` fetches `public/dashboard-config/index.json`
first, then each filename it lists, from `public/dashboard-config/{filename}` in the
dashboard repo — the same discovery pattern as `public/scenarios/index.json`, not a
fixed filename list, and not via the old shared-`.wfrc/`-folder model this replaced.

**URL deep links:** `?s=observed&s=2027-rtp-baseyear` *adds* scenarios to the
active set on load (never removes). On first load with no params, **every scenario
whose data loaded successfully (`status: ready`) is active by default**
(`038-all-loaded-scenarios`) — so unpinned panels render a comparison across all
loaded runs, and the Scenarios-tab Switch excludes any one of them. A scenario
whose folder failed to register (an empty `public/observed/` in a real
deployment, a broken folder) is simply absent — never an error.

---

## `summarize.yaml`

Runs via `wftdm-dashboard summarize --input <raw-activitysim-output-dir>
--config summarize.yaml --output <scenario-dir> --scenario-name <name>`
(025-python-postprocessor) — a subcommand of the same `wftdm-dashboard` CLI
that ships `serve`/`here`/`init`, not a standalone `summarize.py` script (an
earlier draft of this document named that invocation; corrected once the
real pipeline was built — see `specs/025-python-postprocessor/
contracts/cli.md` for the full option list). Produces one named Parquet
file per metric, plus an auto-generated `manifest.yaml`. The same SQL
dialect runs unchanged in DuckDB-WASM in the browser.

### Top-level structure

```yaml
# summarize.yaml (authored alongside model scripts in the TDM repo; never published)
version: 2

# ── Sources ──────────────────────────────────────────────────────────────────
# Raw ActivitySim output files (relative to scenario folder)
# OMX skims: converted via DuckDB h5db extension (fallback: openmatrix Python lib)
# Geometry:  converted to GeoParquet via DuckDB spatial or GeoPandas

sources:
  trips:      trips.csv
  tours:      tours.csv
  persons:    persons.csv
  households: households.csv
  land_use:   land_use.csv
  zones:      zones.parquet       # taz_id, small_district, medium_district,
                                  # large_district, super_district (county)
  skims:      skims.parquet       # converted from OMX before summarize runs

# ── Mappings ─────────────────────────────────────────────────────────────────
# Column recoding → SQL CASE WHEN expressions
# Referenced in metric SQL as: CASE col $mappings.x END

mappings:
  major_trip_mode:
    DRIVEALONEFREE: SOV
    DRIVEALONEPAY:  SOV
    SHARED2FREE:    HOV
    SHARED2PAY:     HOV
    SHARED3FREE:    HOV
    SHARED3PAY:     HOV
    WALK_LOC:       Transit
    WALK_LRF:       Transit
    WALK_EXP:       Transit
    WALK_HVY:       Transit
    WALK_COM:       Transit
    DRIVE_LOC:      Transit
    DRIVE_LRF:      Transit
    DRIVE_EXP:      Transit
    DRIVE_HVY:      Transit
    DRIVE_COM:      Transit
    DRIVEACCESS:    Transit
    WALK:           Non-Motorized
    BIKE:           Non-Motorized
    TAXI:           Ride Hail
    TNC_SINGLE:     Ride Hail
    TNC_SHARED:     Ride Hail

  major_access_mode:
    WALK_LOC:  Walk to Transit
    WALK_LRF:  Walk to Transit
    WALK_EXP:  Walk to Transit
    WALK_HVY:  Walk to Transit
    WALK_COM:  Walk to Transit
    DRIVE_LOC: Drive to Transit
    DRIVE_LRF: Drive to Transit
    DRIVE_EXP: Drive to Transit
    DRIVE_HVY: Drive to Transit
    DRIVE_COM: Drive to Transit

# ── Bins ─────────────────────────────────────────────────────────────────────
# Binning definitions → SQL CASE expressions
# Referenced in metric SQL as: $bins.x AS col_name
# Types: manual_breaks | quantiles | spaced_intervals | equal_intervals

bins:
  income_category:
    column: income
    type:   manual_breaks
    breaks: [0, 25000, 50000, 75000, 100000]
    labels:
      - Very Low Income ($0-$25k)
      - Low Income ($25k-$50k)
      - Medium Income ($50k-$75k)
      - High Income ($75k-$100k)
      - Very High Income (>$100k)

  income_quintile:
    column: income
    type:   quantiles
    bins:   5

  distance_bin_half_mile:
    column:   auto_distance
    type:     spaced_intervals
    interval: 0.5
    lower:    0

  # equal_intervals: N equal-WIDTH buckets computed from the column's actual
  # min/max at query time (data-driven) — unlike spaced_intervals above,
  # whose `interval` is a fixed, known-ahead-of-time absolute width. Useful
  # when the column's range varies by scenario/geography and a fixed
  # interval would produce a different bucket count each time.
  trip_length_quartile:
    column: distance
    type:   equal_intervals
    n:      4
    labels: [Shortest, Short, Long, Longest]

  time_of_day_period:
    column: depart_hour
    type:   manual_breaks
    breaks: [0, 6, 9, 15, 18, 24]
    labels: [EA, AM, MD, PM, EV]

# ── SQL Fragments ─────────────────────────────────────────────────────────────
# Reusable SQL snippets referenced as $sql.x in metric queries.
# trips_merged / tours_merged / persons_merged replace any separate preprocessor
# step — the join and aggregation are one SQL statement in DuckDB.

sql_fragments:

  # Base join tables — include geography levels from zones lookup
  trips_merged: |
    trips t
    JOIN persons p    ON t.person_id    = p.person_id
    JOIN households h ON p.household_id = h.household_id
    JOIN zones z      ON h.home_zone_id = z.taz_id

  tours_merged: |
    tours t
    JOIN persons p    ON t.person_id    = p.person_id
    JOIN households h ON p.household_id = h.household_id
    JOIN zones z      ON h.home_zone_id = z.taz_id

  persons_merged: |
    persons p
    JOIN households h ON p.household_id = h.household_id
    JOIN zones z      ON h.home_zone_id = z.taz_id

  households_merged: |
    households h
    JOIN zones z ON h.home_zone_id = z.taz_id

  # Auto sufficiency derived column — inline in any SELECT
  auto_sufficiency: |
    CASE
      WHEN h.auto_ownership = 0             THEN 'Zero Autos'
      WHEN h.auto_ownership < h.num_workers THEN 'Autos < Workers'
      ELSE                                       'Autos >= Workers'
    END

  # Skim-derived distance — inline in any SELECT joining skims s
  auto_distance_expr: |
    CASE t.trip_mode
      WHEN 'DRIVEALONEFREE' THEN s.SOV_DIST
      WHEN 'DRIVEALONEPAY'  THEN s.SOVTOLL_DIST
      WHEN 'SHARED2FREE'    THEN s.HOV2_DIST
      WHEN 'SHARED2PAY'     THEN s.HOV2TOLL_DIST
      WHEN 'SHARED3FREE'    THEN s.HOV3_DIST
      WHEN 'SHARED3PAY'     THEN s.HOV3TOLL_DIST
      ELSE 0
    END

# ── Metrics ───────────────────────────────────────────────────────────────────
# Each metric → one Parquet file written to summary/{name}.parquet
# SQL placeholders: $mappings.x  $bins.x  $sql.x

metrics:

  # ── Summary KPIs (landing page value boxes) ───────────────────────────────
  - name: summary_kpis
    description: Scalar KPIs for landing page value boxes
    sql: |
      SELECT
        COUNT(DISTINCT h.household_id)                              AS total_households,
        COUNT(DISTINCT p.person_id)                                 AS total_persons,
        COUNT(DISTINCT t.trip_id)                                   AS total_trips,
        SUM($sql.auto_distance_expr)                                AS total_vmt,
        SUM($sql.auto_distance_expr) / COUNT(DISTINCT p.person_id) AS vmt_per_capita,
        COUNT(DISTINCT t.trip_id) * 1.0
          / COUNT(DISTINCT h.household_id)                          AS trips_per_household,
        AVG($sql.auto_distance_expr)                                AS avg_trip_length_miles
      FROM $sql.trips_merged
      JOIN skims s ON t.origin = s.orig AND t.dest = s.dest
                   AND t.time_period = s.period

  # ── Person / Household models ─────────────────────────────────────────────
  - name: auto_ownership
    description: Households by auto ownership × segmentation
    sql: |
      SELECT
        h.auto_ownership,
        h.num_persons   AS household_size,
        h.num_workers,
        $bins.income_category AS income_category,
        z.super_district,
        COUNT(*) AS households
      FROM $sql.households_merged
      GROUP BY h.auto_ownership, household_size, h.num_workers,
               income_category, z.super_district

  - name: work_from_home
    description: Workers by WFH status × segmentation
    sql: |
      SELECT
        p.is_worker,
        p.work_from_home,
        $bins.income_category AS income_category,
        p.age,
        p.sex,
        z.super_district,
        COUNT(*) AS persons
      FROM $sql.persons_merged
      WHERE p.is_worker = 1
      GROUP BY p.work_from_home, income_category, p.age, p.sex, z.super_district

  - name: transit_pass_subsidy
    description: Persons by transit pass subsidy × person type
    sql: |
      SELECT
        p.person_type,
        p.transit_pass_subsidy,
        COUNT(*) AS persons
      FROM $sql.persons_merged
      GROUP BY p.person_type, p.transit_pass_subsidy

  - name: transit_pass_ownership
    description: Persons by transit pass ownership × segmentation
    sql: |
      SELECT
        p.person_type,
        $bins.income_category AS income_category,
        $sql.auto_sufficiency AS auto_sufficiency,
        p.transit_pass_ownership,
        COUNT(*) AS persons
      FROM $sql.persons_merged
      GROUP BY p.person_type, income_category, auto_sufficiency,
               p.transit_pass_ownership

  - name: telecommute_freq
    description: Workers by telecommute frequency × segmentation
    sql: |
      SELECT
        p.telecommute_frequency,
        $bins.income_category AS income_category,
        p.age,
        p.sex,
        p.occupation,
        CASE WHEN p.has_children = 1 THEN 'Has Children' ELSE 'No Children' END AS children,
        z.super_district,
        COUNT(*) AS persons
      FROM $sql.persons_merged
      WHERE p.is_worker = 1
      GROUP BY p.telecommute_frequency, income_category, p.age, p.sex,
               p.occupation, children, z.super_district

  - name: cdap
    description: Persons by coordinated daily activity pattern × segmentation
    sql: |
      SELECT
        p.person_type,
        p.cdap_activity,
        $sql.auto_sufficiency AS auto_sufficiency,
        h.num_persons AS household_size,
        COUNT(*) AS persons
      FROM $sql.persons_merged
      GROUP BY p.person_type, p.cdap_activity, auto_sufficiency, household_size

  - name: school_location_dist
    description: School location distance distribution by school segment
    sql: |
      SELECT
        p.school_segment,
        FLOOR(p.distance_to_school / 0.5) * 0.5 AS distance_bin,
        AVG(p.distance_to_school)                AS avg_distance,
        COUNT(*) AS persons
      FROM $sql.persons_merged
      WHERE p.is_student = 1
      GROUP BY p.school_segment, distance_bin

  - name: workplace_location_dist
    description: Workplace location distance distribution
    sql: |
      SELECT
        FLOOR(p.distance_to_work / 0.5) * 0.5 AS distance_bin,
        AVG(p.distance_to_work)                AS avg_distance,
        COUNT(*) AS persons
      FROM $sql.persons_merged
      WHERE p.is_worker = 1
      GROUP BY distance_bin

  - name: workplace_od_flows
    description: Home-to-workplace flows for county-to-county O-D map
    sql: |
      SELECT
        z_home.super_district  AS home_district,
        z_work.super_district  AS work_district,
        COUNT(*)               AS workers
      FROM persons p
      JOIN households h   ON p.household_id  = h.household_id
      JOIN zones z_home   ON h.home_zone_id  = z_home.taz_id
      JOIN zones z_work   ON p.workplace_zone_id = z_work.taz_id
      WHERE p.is_worker = 1
      GROUP BY home_district, work_district
      HAVING workers >= 5

  # ── Tour models ───────────────────────────────────────────────────────────
  - name: mandatory_tour_freq
    description: Mandatory tour frequency by purpose and person type
    sql: |
      SELECT
        p.person_type,
        t.tour_purpose,
        COUNT(*) AS tours
      FROM $sql.tours_merged
      WHERE t.tour_category = 'mandatory'
      GROUP BY p.person_type, t.tour_purpose

  - name: mandatory_tour_scheduling
    description: Mandatory tours by start/end hour and purpose
    sql: |
      SELECT
        t.tour_purpose,
        FLOOR(t.start_time / 60) AS start_hour,
        FLOOR(t.end_time   / 60) AS end_hour,
        (t.end_time - t.start_time) / 60.0 AS duration_hours,
        COUNT(*) AS tours
      FROM tours t
      WHERE t.tour_category = 'mandatory'
      GROUP BY t.tour_purpose, start_hour, end_hour, duration_hours

  - name: joint_tour_freq
    description: Joint tour frequency by purpose
    sql: |
      SELECT
        t.tour_purpose,
        COUNT(*) AS tours
      FROM tours t
      WHERE t.tour_category = 'joint'
      GROUP BY t.tour_purpose

  - name: joint_tour_participation
    description: Joint tour participation counts
    sql: |
      SELECT
        COUNT(*) AS tours,
        AVG(t.number_of_participants) AS avg_participants
      FROM tours t
      WHERE t.tour_category = 'joint'

  - name: joint_tour_destination_dist
    description: Joint tour destination distance distribution
    sql: |
      SELECT
        FLOOR(t.distance / 0.5) * 0.5 AS distance_bin,
        AVG(t.distance)               AS avg_distance,
        COUNT(*) AS tours
      FROM tours t
      WHERE t.tour_category = 'joint'
      GROUP BY distance_bin

  - name: joint_tour_scheduling
    description: Joint tours by start/end hour and purpose
    sql: |
      SELECT
        t.tour_purpose,
        FLOOR(t.start_time / 60) AS start_hour,
        FLOOR(t.end_time   / 60) AS end_hour,
        COUNT(*) AS tours
      FROM tours t
      WHERE t.tour_category = 'joint'
      GROUP BY t.tour_purpose, start_hour, end_hour

  - name: non_mandatory_tour_freq
    description: Non-mandatory tour frequency by purpose and person type
    sql: |
      SELECT
        p.person_type,
        t.tour_purpose,
        COUNT(*) AS tours
      FROM $sql.tours_merged
      WHERE t.tour_category = 'non_mandatory'
      GROUP BY p.person_type, t.tour_purpose

  - name: non_mandatory_tour_destination_dist
    description: Non-mandatory tour destination distance distribution by purpose
    sql: |
      SELECT
        t.tour_purpose,
        FLOOR(t.distance / 0.5) * 0.5 AS distance_bin,
        AVG(t.distance)               AS avg_distance,
        COUNT(*) AS tours
      FROM tours t
      WHERE t.tour_category = 'non_mandatory'
      GROUP BY t.tour_purpose, distance_bin

  - name: non_mandatory_tour_scheduling
    description: Non-mandatory tours by start/end hour and purpose
    sql: |
      SELECT
        t.tour_purpose,
        FLOOR(t.start_time / 60) AS start_hour,
        FLOOR(t.end_time   / 60) AS end_hour,
        COUNT(*) AS tours
      FROM tours t
      WHERE t.tour_category = 'non_mandatory'
      GROUP BY t.tour_purpose, start_hour, end_hour

  - name: atwork_subtour_freq
    description: At-work subtour frequency by purpose
    sql: |
      SELECT
        t.tour_purpose,
        COUNT(*) AS tours
      FROM tours t
      WHERE t.tour_category = 'atwork'
      GROUP BY t.tour_purpose

  - name: atwork_subtour_destination_dist
    description: At-work subtour destination distance distribution by purpose
    sql: |
      SELECT
        t.tour_purpose,
        FLOOR(t.distance / 0.5) * 0.5 AS distance_bin,
        AVG(t.distance)               AS avg_distance,
        COUNT(*) AS tours
      FROM tours t
      WHERE t.tour_category = 'atwork'
      GROUP BY t.tour_purpose, distance_bin

  - name: atwork_subtour_scheduling
    description: At-work subtours by start/end time period
    sql: |
      SELECT
        FLOOR(t.start_time / 60) AS start_hour,
        FLOOR(t.end_time   / 60) AS end_hour,
        COUNT(*) AS tours
      FROM tours t
      WHERE t.tour_category = 'atwork'
      GROUP BY start_hour, end_hour

  - name: stop_frequency
    description: Stop frequency by tour purpose and direction
    sql: |
      SELECT
        t.tour_purpose,
        t.direction,
        t.num_stops,
        COUNT(*) AS tours
      FROM tours t
      GROUP BY t.tour_purpose, t.direction, t.num_stops

  # ── Mode choice ───────────────────────────────────────────────────────────
  - name: tour_mode_share
    description: Tour mode share × purpose × person type × income × auto sufficiency
    sql: |
      SELECT
        t.tour_purpose,
        CASE t.tour_mode $mappings.major_trip_mode END AS mode,
        p.person_type,
        p.sex,
        $bins.income_category                          AS income_category,
        $sql.auto_sufficiency                          AS auto_sufficiency,
        COUNT(*) AS tours,
        COUNT(*) * 1.0
          / SUM(COUNT(*)) OVER (PARTITION BY t.tour_purpose) AS share
      FROM $sql.tours_merged
      GROUP BY t.tour_purpose, mode, p.person_type, p.sex,
               income_category, auto_sufficiency

  - name: atwork_subtour_mode
    description: At-work subtour mode share by purpose
    sql: |
      SELECT
        t.tour_purpose,
        CASE t.tour_mode $mappings.major_trip_mode END AS mode,
        COUNT(*) AS tours,
        COUNT(*) * 1.0
          / SUM(COUNT(*)) OVER (PARTITION BY t.tour_purpose) AS share
      FROM tours t
      WHERE t.tour_category = 'atwork'
      GROUP BY t.tour_purpose, mode

  - name: trip_mode_share
    description: Trip mode share × purpose × tour mode × income × auto sufficiency
    sql: |
      SELECT
        t.primary_purpose                              AS purpose,
        CASE t.trip_mode $mappings.major_trip_mode END AS mode,
        CASE t.tour_mode $mappings.major_trip_mode END AS tour_mode,
        $bins.income_category                          AS income_category,
        $sql.auto_sufficiency                          AS auto_sufficiency,
        COUNT(*) AS trips,
        COUNT(*) * 1.0
          / SUM(COUNT(*)) OVER (PARTITION BY t.primary_purpose) AS share
      FROM $sql.trips_merged
      GROUP BY purpose, mode, tour_mode, income_category, auto_sufficiency

  - name: tour_mode_to_trip_mode
    description: Tour-to-trip mode consistency (Sankey source)
    sql: |
      SELECT
        CASE t.tour_mode $mappings.major_trip_mode END AS tour_mode,
        CASE t.trip_mode $mappings.major_trip_mode END AS trip_mode,
        COUNT(*) AS trips
      FROM trips t
      GROUP BY tour_mode, trip_mode
      ORDER BY trips DESC

  # ── Trip models ───────────────────────────────────────────────────────────
  - name: trip_purpose
    description: Trips by purpose × tour purpose
    sql: |
      SELECT
        t.primary_purpose  AS trip_purpose,
        t.tour_purpose,
        COUNT(*) AS trips
      FROM trips t
      GROUP BY trip_purpose, t.tour_purpose

  - name: trip_destination_dist
    description: Trip destination distance distribution by purpose
    sql: |
      SELECT
        t.primary_purpose AS purpose,
        FLOOR($sql.auto_distance_expr / 0.5) * 0.5 AS distance_bin,
        AVG($sql.auto_distance_expr)                AS avg_distance,
        COUNT(*) AS trips
      FROM $sql.trips_merged
      JOIN skims s ON t.origin = s.orig AND t.dest = s.dest
                   AND t.time_period = s.period
      GROUP BY purpose, distance_bin

  - name: trip_scheduling
    description: Trips by start/end hour and purpose
    sql: |
      SELECT
        t.primary_purpose AS purpose,
        FLOOR(t.depart / 60)  AS start_hour,
        FLOOR(t.arrive / 60)  AS end_hour,
        (t.arrive - t.depart) / 60.0 AS duration_hours,
        COUNT(*) AS trips
      FROM trips t
      GROUP BY purpose, start_hour, end_hour, duration_hours

  # ── Network / assignment ──────────────────────────────────────────────────
  - name: screenlines
    description: Screenline volumes vs observed AADT
    sql: |
      SELECT
        a.link_id,
        a.facility_type,
        a.direction,
        a.volume                                          AS modeled,
        o.aadt                                            AS observed,
        a.volume - o.aadt                                 AS diff,
        (a.volume - o.aadt) * 1.0 / NULLIF(o.aadt, 0)   AS pct_error
      FROM assignment a
      JOIN observed_counts o ON a.link_id = o.link_id

  - name: vmt_by_facility
    description: VMT by facility type
    sql: |
      SELECT
        facility_type,
        SUM(volume * length_miles) AS vmt
      FROM assignment
      GROUP BY facility_type

  - name: vmt_by_home_taz
    description: VMT per capita by home TAZ for zone choropleth map
    sql: |
      WITH vmt AS (
        SELECT h.home_zone_id,
               SUM($sql.auto_distance_expr) AS total_vmt
        FROM $sql.trips_merged
        JOIN skims s ON t.origin = s.orig AND t.dest = s.dest
                     AND t.time_period = s.period
        GROUP BY h.home_zone_id
      ),
      pop AS (
        SELECT home_zone_id, COUNT(*) AS persons
        FROM persons p
        JOIN households h ON p.household_id = h.household_id
        GROUP BY home_zone_id
      )
      SELECT
        v.home_zone_id AS taz_id,
        v.total_vmt / NULLIF(p.persons, 0) AS vmt_per_capita
      FROM vmt v JOIN pop p ON v.home_zone_id = p.home_zone_id

  - name: od_flows
    description: Aggregate O-D trip flows for flowmap desire lines
    sql: |
      SELECT
        t.origin          AS orig_taz,
        t.destination     AS dest_taz,
        t.primary_purpose AS purpose,
        CASE t.trip_mode $mappings.major_trip_mode END AS mode,
        SUM(t.number_of_participants) AS trips
      FROM trips t
      GROUP BY orig_taz, dest_taz, purpose, mode
      HAVING SUM(t.number_of_participants) >= 5
```

### How placeholders expand

**`$mappings.major_trip_mode`** expands to WHEN clauses inside a CASE block:

```sql
-- Written in summarize.yaml metric SQL:
CASE t.trip_mode $mappings.major_trip_mode END

-- Expands to:
CASE t.trip_mode
  WHEN 'DRIVEALONEFREE' THEN 'SOV'
  WHEN 'DRIVEALONEPAY'  THEN 'SOV'
  WHEN 'SHARED2FREE'    THEN 'HOV'
  ...
  ELSE 'Other'
END
```

**`$bins.income_category`** expands to a CASE expression:

```sql
-- Written:
$bins.income_category AS income_category

-- Expands to:
CASE
  WHEN income < 25000  THEN 'Very Low Income ($0-$25k)'
  WHEN income < 50000  THEN 'Low Income ($25k-$50k)'
  WHEN income < 75000  THEN 'Medium Income ($50k-$75k)'
  WHEN income < 100000 THEN 'High Income ($75k-$100k)'
  ELSE 'Very High Income (>$100k)'
END AS income_category
```

**`$sql.trips_merged`** expands to a FROM clause (no leading FROM keyword):

```sql
-- Written:
FROM $sql.trips_merged

-- Expands to:
FROM trips t
JOIN persons p    ON t.person_id    = p.person_id
JOIN households h ON p.household_id = h.household_id
JOIN zones z      ON h.home_zone_id = z.taz_id
```

---

## `dashboard-*.yaml`

Defines one dashboard tab. Files are discovered by the app in alphabetical order.
The first file (`dashboard-1-summary.yaml`) is automatically rendered as the landing
page when a scenario folder is loaded.

### Top-level structure

```yaml
# dashboard-1-summary.yaml (authored in TDM repo; published to public/dashboard-config/)

header:
  tab:         Summary           # sidebar item label
  title:       Model Run Summary # page title
  description: High-level calibration KPIs and overview charts  # optional
  icon:        route             # optional (030-sidebar-navigation) — a lucide-react
                                  # icon name, kebab-case, resolved the same way
                                  # type: valuebox's own icon: field already is. Omit
                                  # for no icon on this sidebar item — never a
                                  # default/placeholder substitute.
  full_page:   false              # optional (030-sidebar-navigation), default false —
                                  # see "Chromeless full-page panel mode" below

# Global sidebar filters — drive reactive updates across ALL panels on this tab
filters:
  - id:      purpose
    label:   Trip Purpose
    type:    select             # select | multiselect | range
    source:  trip_mode_share    # which metric to pull distinct values from
    column:  purpose
    default: all
    all_option: true            # include an "All" option

  - id:      mode
    label:   Mode
    type:    multiselect
    source:  trip_mode_share
    column:  mode
    default: all

# Optional (011-basemap-style-system) — tab-level default basemap for every
# map-rendering panel on this tab that doesn't set its own `basemap:`. Same
# scope as header:/filters: (this one tab only, not shared across tabs).
# Accepts a bare preset name or a composition object — see "Basemap style
# system" under type: flowmap for the full grammar and precedence rules.
default_basemap: carto-voyager

# Optional (030-sidebar-navigation) — tab-level accordion sub-navigation.
# While this tab is the active sidebar item, each entry below shows as an
# expandable sub-item beneath it; clicking one scrolls the page to that
# section's own content. Absent or empty — the default, unchanged
# appearance for every dashboard-*.yaml today — shows no sub-nav at all.
# `rows` names one or more of THIS tab's own `layout:` row keys (below) —
# grouping never reorders them; row render order always stays exactly
# `layout`'s own key order. A `rows` entry naming a row this tab doesn't
# have is dropped with a console warning, never a crash. A `layout` row
# not referenced by any section still renders normally — it simply has no
# sidebar sub-nav entry pointing at it.
sections:
  - id:    kpis
    label: KPIs
    rows:  [row_kpis]
  - id:    charts
    label: Charts & Tables
    rows:  [row_charts, row_tlfd]

# Layout — ordered rows of panels
layout:
  row_kpis:
    - type:    valuebox
      title:   Total Person Trips
      metric:  summary_kpis
      column:  total_trips
      format:  "{:,.0f}"
      icon:    person-walking
      width:   0.25

    - type:    valuebox
      title:   VMT per Capita
      metric:  summary_kpis
      column:  vmt_per_capita
      format:  "{:.1f}"
      unit:    miles/person/day
      observed: 22.4
      threshold_warn: 2.0
      threshold_fail: 5.0
      width:   0.25

    - type:    valuebox
      title:   Auto Mode Share
      metric:  summary_kpis
      column:  auto_share
      format:  "{:.1%}"
      observed: 0.847
      threshold_warn: 0.02
      threshold_fail: 0.05
      width:   0.25

    - type:    valuebox
      title:   Transit Mode Share
      metric:  summary_kpis
      column:  transit_share
      format:  "{:.1%}"
      observed: 0.047
      threshold_warn: 0.02
      width:   0.25

  row_charts:
    - type:    plotly
      title:   Mode Share by Purpose — All Scenarios vs Observed
      metric:  trip_mode_share
      filter:  $filters.purpose
      height:  400
      traces:
        - type:    bar
          x:       $metric.purpose
          y:       $metric.share
          color:   $metric.mode
          barmode: group
      observed:
        source: observed_mode_share
        x:      purpose
        y:      share
        color:  mode
        style:  dot
        name:   Observed
      width: 0.6

    - type:    table
      title:   KPI Summary
      metric:  summary_kpis
      columns:
        - { field: metric,    label: Metric }
        - { field: modeled,   label: Modeled,  format: ".3g" }
        - { field: observed,  label: Observed, format: ".3g" }
        - { field: diff_pct,  label: "% Diff", format: "+.1%",
            color_scale: diverging, domain: [-0.1, 0.1] }
      width: 0.4

  row_tlfd:
    - type:   observable-plot
      title:  Trip Length Frequency Distribution
      metric: trip_destination_dist
      height: 350
      inputs:
        - id:      mode_select
          label:   Mode
          type:    select
          column:  mode
          default: SOV
      filter:
        purpose: $filters.purpose
        mode:    $inputs.mode_select
      mark:    lineY
      x:       distance_bin
      y:       trips
      stroke:  purpose
      tip:     true
      grid:    true
      width:   1.0
```

### Chromeless full-page panel mode (030-sidebar-navigation)

`header.full_page: true` opts a tab into a chromeless, edge-to-edge render
mode — for a tab whose whole purpose is one open-ended workspace (the
canonical example: a `type: graphic-walker` exploration tab), not a
multi-panel dashboard page.

```yaml
header:
  tab:       Explore
  title:     Explore the Demo Data
  icon:      compass
  full_page: true

layout:
  row_explore:
    - type:    graphic-walker
      title:   Free-form Visual Analytics
      dataset: trip_mode_share
      width:   1.0
```

Requirements and behavior:

- `layout` must resolve to **exactly one panel** across all its rows for the
  flag to take effect. Zero or two-or-more panels falls back to the
  ordinary multi-row, card-chromed rendering below — a config-authoring
  warning (logged to the console), never a blank or broken page.
- When effective: no page-title block (`header.title`/`header.description`
  are simply never rendered on this path — not blanked, just not
  consulted), no panel `Card` border/shadow/rounded-corners/title bar, and
  no expand-to-dialog control (the panel is already rendered at its
  maximum useful size).
- The single panel's own available height tracks real, current viewport
  space — not a fixed pixel value, not a library-intrinsic default.
- Generic across panel types at the mechanism level — any panel type may be
  configured this way; `graphic-walker` is simply this app's own current
  real-world example, not a requirement.

---

## Panel types reference

All panels share these common keys:

```yaml
type:      <string>     # required — see table below
title:     <string>     # card header
metric:    <string>     # Parquet file name (without .parquet) to query
filter:    <inline | $ref>  # SQL WHERE bindings or reference to global filter
height:    <pixels>     # default: 350
width:     <0.0–1.0>    # fraction of row width; panels in a row should sum to 1.0
scenario:  <string>     # optional — query only this scenario view; omit for N-way
scenarios: [a, b]       # optional — override global scenario list for this panel
expandable: <bool>      # optional (034-metric-panel-redesign) — overrides this
                        # panel's own expand-to-dialog affordance; omit to use its
                        # type's own default (see below)
```

**`expandable:` (034-metric-panel-redesign)** — every panel type gets an
expand-to-dialog control by DEFAULT, except `type: valuebox` and an ordinary
(non-full-page-Explore) `type: graphic-walker`, which default to none — a
value box has nothing more to show at a larger size, and a full-page Explore
panel already bypasses this mechanism entirely on its own dedicated render
path. Set `expandable: true`/`expandable: false` on any single panel to
override its own type's default in either direction — e.g. opt one specific
`valuebox` panel back in, or opt one specific `table` panel out — with no
effect on any other panel of the same type. Omit it entirely (every
`dashboard-*.yaml` file authored before this field existed) to keep the
type's own default unchanged. Has no effect on a panel rendered via the
chromeless full-page Explore mode (below) — that path has no expand
mechanism to turn on or off.

**Twelfths convention (030-sidebar-navigation, authoring guidance only — no
runtime behavior change)**: author `width:` as a fraction of twelfths — e.g.
`0.5` = 6/12 half-width, `0.25` = 3/12 quarter-width, `0.3333` ≈ 4/12
third-width — so panels in DIFFERENT rows on the same tab, when authored to
the same twelfths scheme, align their column boundaries vertically down the
page. This is purely a naming convention for values the grid math already
supports today; any fraction has always worked.

**Metric Strip (030-sidebar-navigation, automatic — not an authored field)**:
a row whose every panel is `type: valuebox` automatically renders as an
auto-filling grid of minimum-width (200px) cards instead of the ordinary
fraction-based columns above. Nothing to opt into — it's a direct,
deterministic consequence of a row's own panel-type composition. Every other
row composition is completely unaffected.

### `type: valuebox`

Single scalar KPI card with optional observed reference and threshold coloring.

```yaml
- type:      valuebox
  title:     Auto Mode Share
  metric:    summary_kpis
  column:    auto_share          # which column from the metric to display
  format:    "{:.1%}"           # Python-style format string
  unit:      ""                 # shown as subtitle unit label
  icon:      car                # lucide icon name
  observed:  0.847              # reference value shown below the main value
  threshold_warn: 0.02          # absolute diff from observed → yellow
  threshold_fail: 0.05          # absolute diff from observed → red
```

Multi-scenario: auto renders one column per loaded scenario + observed column.

### `type: plotly`

Plotly chart — default for most calibration panels. Provides interactive legend,
fullscreen, PNG export, and formatted hover tooltips. Use `Plotly.react()` internally.

```yaml
- type:    plotly
  title:   Volume vs Count Scatter
  metric:  screenlines
  height:  500
  traces:
    - type:  scatter
      x:     $metric.observed
      y:     $metric.modeled
      mode:  markers
      name:  $scenario           # auto-replaced per loaded scenario
      text:  $metric.link_id    # hover label
  reference_lines:
    - type:  diagonal            # y = x perfect fit line
    - type:  band
      slope: 1
      offset_pct: [10, -10]     # ±10% error bands
      style: { dash: dot, color: grey }
  observed:
    source: observed_counts
    style:  scatter
  layout:
    xaxis: { title: Observed AADT, type: log }
    yaxis: { title: Modeled Volume, type: log }
    showlegend: true
```

Also accepts `comparison:`/`compare_on:` (019-baseline-diff-consumption) — see "Scenario comparison / diff mode" under `type: zonemap` below, the shared grammar all four comparison-capable panel types use identically.

### `type: observable-plot`

Observable Plot — use when the panel has user-controlled filter inputs that update
the chart reactively. Inputs render as controls inside the panel card.

```yaml
- type:    observable-plot
  title:   Mode Share by Income Group
  metric:  trip_mode_share
  height:  350
  inputs:
    - id:      income_filter
      label:   Income Group
      type:    multiselect
      column:  income_category
  filter:
    purpose: $filters.purpose    # bound to global sidebar filter
    income_category: $inputs.income_filter
  mark:    barY
  x:       income_category
  y:       share
  fill:    mode
  facet_x: mode                  # small multiples by mode
  tip:     true
  width:   1.0
```

Also accepts `comparison:`/`compare_on:` (019-baseline-diff-consumption) — see "Scenario comparison / diff mode" under `type: zonemap` below, the shared grammar all four comparison-capable panel types use identically. A row whose `diff_value` is `NULL` (the zero-baseline percent-diff case) is cleanly omitted from the rendered marks, never coerced to `0`.

### `type: recharts` (029-shadcn-chart-panel)

shadcn/ui's official chart component — a themed layer over Recharts using
this app's own CSS custom-property token system (`--chart-1`..`--chart-5`,
verified WFRC-brand-consistent, WCAG-checked in both themes). This is the
new **default/primary** engine for bar/line/area charts going forward —
use `type: plotly` or `type: observable-plot` instead only when you need
something this panel type doesn't do (see "Explicitly out of scope"
below).

```yaml
- type:       recharts
  title:      Mode Share by Purpose
  metric:     trip_mode_share
  chart_type: bar            # 'bar' | 'line' | 'area'
  x:          purpose
  y:          share
  series:     mode           # optional — omit for a single-series chart
  stacked:    false          # optional, default false
  scenario:   good_scenario  # optional, same meaning as every other type
  filter:     purpose        # optional, same $ref/inline-map grammar
  comparison: side_by_side   # optional, same grammar as plotly/table/observable-plot
  height:     350
  width:      1.0
```

`series` (optional) pivots tidy query rows into one Recharts series per
distinct value — omit it for a single-series chart bound directly to `y`.
Colors cycle `--chart-1`..`--chart-5` in first-seen (query result) order,
wrapping past the fifth distinct series value. `stacked: true` sets a
shared `stackId` across every series, stacking bar/area marks instead of
grouping them side by side. Tooltips and (when more than one series is
present) a legend render automatically with zero extra author
configuration (US3) — real, on-brand, theme-correct in both light and
dark mode with no `tip:`/`legend:` key needed, unlike `type: observable-
plot`'s own explicit `tip: true`.

Also accepts `comparison:`/`compare_on:` (019-baseline-diff-consumption)
— see "Scenario comparison / diff mode" under `type: zonemap` below, the
same shared grammar every other comparison-capable panel type uses.

**What this panel type does NOT do** — use `type: plotly` or `type:
observable-plot` instead for these:

- Sankey diagrams — use `type: sankey`. `chart_type` has no `'sankey'`
  option at all; this is enforced by the type union itself.
- Interactive per-category legend click-to-toggle-trace-visibility —
  shadcn's shipped `ChartLegendContent` wires up no click handler, despite
  Recharts' own `Legend` exposing a real `onClick` prop (a real, closed
  upstream request, `shadcn-ui/ui#4188`). `type: plotly`'s own legend
  already does this and is unaffected — keep using it where this
  capability matters.
- `pie`/`radar`/`radial` charts — not in the `chart_type` union in this
  version, even though shadcn's own component supports them.
- Panel-local reactive `inputs:` (Observable Plot's own capability) — not
  part of this panel type's grammar.

### `type: table`

Sortable, paginated data table. Supports inline column expressions and color scales.

```yaml
- type:       table
  title:      Screenline Validation
  metric:     screenlines
  sort:       { column: pct_error, order: desc }
  pagination: 20
  searchable: true
  columns:
    - { field: link_id,       label: Link ID }
    - { field: facility_type, label: Facility }
    - { field: observed,      label: Observed, format: ",.0f" }
    - { field: modeled,       label: Modeled,  format: ",.0f" }
    - { field: pct_error,     label: "% Error", format: "+.1%",
        color_scale: diverging, domain: [-0.5, 0.5] }
```

Also accepts `comparison:`/`compare_on:` (019-baseline-diff-consumption) — see "Scenario comparison / diff mode" under `type: zonemap` below, the shared grammar all four comparison-capable panel types use identically. The computed result is always named `diff_value` — reference it like any other queried column, e.g. `{ field: diff_value, label: "Diff", format: ",.1f" }`.

### `type: flowmap`

flowmap.gl on MapLibre — O-D desire lines with zoom-adaptive clustering.
O-D data must be pre-aggregated in the Parquet file, with origin/
destination coordinates already resolved to lat/lon columns on each row
— no raw trip-level data, and no live GeoParquet/DuckDB-spatial
zone-boundary join in the browser. Zone-centroid resolution is an
offline concern, done once when the metric Parquet is built (see
`docs/ARCHITECTURE.md`'s "all format conversion... happens offline —
the browser only ever reads Parquet" principle) — the same offline/
online split every other geometry conversion in this system already
follows, not a new exception carved out for flowmap.

**Correction (verified against two real production WFRC reference
apps — `APP-Commute-Explorer`'s `FlowLayer.jsx` and
`APP-WFRC-Commute-Patterns`' `map.js`, both fetched and read directly,
not assumed): neither resolves zone geometry via a live GeoParquet join
in the browser.** Both construct `FlowmapLayer`'s `locations` from
lat/lon values already present as plain columns on each flow row
(`f.home_lat`/`f.home_lon`/`f.work_lat`/`f.work_lon` in
`APP-WFRC-Commute-Patterns`; the equivalent `loc.lat`/`loc.lon` shape in
`APP-Commute-Explorer`). An earlier version of this grammar had
`boundaries`/`boundaries_id` keys describing a live GeoParquet zone
join instead — that design was never actually built this way in either
real reference implementation, so it's corrected here to match the
proven pattern rather than left describing an unbuilt, untested
alternative. Field names below are author-configurable, matching the
established field-mapping convention every other panel type already
uses (`TableColumnConfig.field`, `SankeyPanelConfig`'s
`source`/`target`/`value`, `ObservablePlotPanelConfig`'s
`x`/`y`/`fill`/`stroke`) — not a new binding shape.

```yaml
- type:        flowmap
  title:       Trip Distribution Desire Lines
  metric:      od_flows
  height:      500
  origin:      orig_taz        # column: origin zone id/name (location id)
  origin_lat:  orig_lat        # column: origin latitude
  origin_lon:  orig_lon        # column: origin longitude
  destination: dest_taz        # column: destination zone id/name
  dest_lat:    dest_lat        # column: destination latitude
  dest_lon:    dest_lon        # column: destination longitude
  value:       trips           # column for flow magnitude
  clustering:      true
  clustering_auto: true
  animation:       false
  max_flows:       500
  filter:  $filters.purpose
  center:  [-111.89, 40.76]
  zoom:    9
  basemap: carto-voyager        # optional (011-basemap-style-system) — see "Basemap style system" below
```

#### Basemap style system (011-basemap-style-system)

Every map-rendering panel type (`flowmap` today; `zonemap` once built)
accepts an optional `basemap:` key. When omitted, the panel renders a
built-in default automatically paired to the dashboard's own light/dark
theme (`carto-positron` in light mode, `carto-dark-matter` in dark mode)
— no author configuration required.

`basemap:` (panel-level) and `default_basemap:` (tab-level — see
`header:`/`filters:` below, same scope) both accept the same two shapes:

**A bare preset name** (string) — a built-in style from one of three
source categories:

```yaml
basemap: carto-positron        # CARTO: carto-positron | carto-dark-matter | carto-voyager
basemap: openfreemap-liberty   # OpenFreeMap: openfreemap-liberty | -bright | -positron | -dark | -fiord
basemap: OpenTopoMap           # leaflet-providers raster catalog, resolved dynamically by
basemap: CartoDB.Positron      # name — dotted provider.variant form where the provider has variants
```

**A composition object** — stacks multiple independently-hosted style
resources into one basemap (bottom layer first), for a basemap not in
the built-in registry above (e.g. a state/regional GIS agency's own
multi-layer vector service):

```yaml
basemap:
  layers:
    - https://example.gis.gov/services/Hillshade/VectorTileServer/resources/styles/root.json
    - https://example.gis.gov/services/BaseMap/VectorTileServer/resources/styles/root.json
    - https://example.gis.gov/services/Labels/VectorTileServer/resources/styles/root.json
```

**Precedence** (panel > tab > app default): a panel's own `basemap:`
wins if set; else its tab's `default_basemap:` wins if set; else the
theme-paired default applies. An explicit `basemap:`/`default_basemap:`
pin (either shape, either scope) is NOT re-paired when the dashboard's
theme changes — it means that exact basemap regardless of theme. Only
the no-config, theme-paired default reacts live to a theme change.

If a configured or default basemap fails to load (unreachable host,
unrecognized name, a composition with a broken layer), the panel falls
back to a blank background — the data-driven overlay (e.g. flow lines)
always still renders.

### `type: zonemap`

MapLibre choropleth — zone-level metric joined to TAZ GeoParquet geometry.

```yaml
- type:       zonemap
  title:      VMT per Capita by Home TAZ
  metric:     vmt_by_home_taz
  height:     450
  boundaries:    taz.geoparquet
  boundaries_id: TAZ_ID
  metric_id:     taz_id         # matching column in metric Parquet
  column:     vmt_per_capita    # which column to display
  label:      VMT / Capita
  color_scale: sequential
  color_ramp:  YlOrRd
  steps:       7
  domain:      [0, 40]          # optional fixed domain; omit for auto
  comparison:  side_by_side     # side_by_side | diff
  center:  [-111.89, 40.76]
  zoom:    9
```

**Diff mode:**
```yaml
  comparison:
    type: diff
    a:    base_tbm
    b:    abm_2026
    expr: "b.vmt_per_capita - a.vmt_per_capita"
  color_scale: diverging
  color_ramp:  RdBu
  domain: [-5, 5]
```

#### Scenario comparison / diff mode (013-zonemap-panel, extended by 019-baseline-diff-consumption)

`comparison:`/`compare_on:` — one shared grammar, identically shaped and named, across all four comparison-capable panel types: `zonemap` (above), `plotly`, `table`, and `observable-plot`. `comparison:` accepts `side_by_side` (the default — a panel's own single selected/active scenario, unchanged) or a `diff` object:

```yaml
comparison:
  type: diff
  a:    base_tbm      # OR the literal string '$baseline' — see below
  b:    abm_2026       # OR '$baseline'
  expr: "b.vmt_per_capita - a.vmt_per_capita"   # any SQL expression, referencing the a/b table aliases
compare_on: [taz_id]   # required for plotly/table/observable-plot; optional for
                        # zonemap, where it defaults to metric_id (existing configs
                        # need no change)
```

- **`a`/`b`** each name a scenario, either a literal hardcoded name (unchanged since `013`) or the literal string `$baseline` — a sentinel resolved at query time to whichever scenario currently holds the baseline designation (`018-baseline-scenario-designation`, set via the scenario list UI). A `$baseline`-referencing panel automatically recomputes when a viewer changes which scenario is baseline — no reload, no config change needed.
- **`compare_on`** names the column(s) that jointly identify a "comparable row" between the two sides — every column a chart wants to bind (`x`/`y`/`fill`/`field`/etc.) on a diff view must also appear in `compare_on`, since the diff is only meaningful when both sides are matched on those exact columns. Rows present on only one side are excluded (an inner join), matching `zonemap`'s own original behavior.
- The computed result is always a column named **`diff_value`** — reference it through each panel type's own ordinary field-binding grammar (`column:`, `field:`, `x:`/`y:`/`color:`, `x:`/`y:`/`fill:`/`stroke:`) exactly like any other queried column. No new binding key exists for it.
- A **percent-difference** formula is authored the same way any other `expr:` is — no separate mode. Guard against a zero denominator with SQL's own `NULLIF(<denominator>, 0)` (the same idiom `summarize.yaml`'s own `screenlines` metric already uses for `pct_error`), which yields `NULL` rather than `Infinity`/`NaN`. Every panel type treats a `NULL` `diff_value` as a distinct, defined "not computable" state (a table cell shows `N/A` with a dedicated color; a chart mark is cleanly omitted) — never silently `0`.
- `comparison: diff` (any `a`/`b` combination) does **not** apply the panel's own `filter:` bindings — this was already `zonemap`'s existing, deliberate behavior and is unchanged here.
- If `$baseline` is used and no scenario currently resolves as baseline, the panel shows its ordinary error state — the query is never built or run against an unresolved name.

### `type: sankey`

d3-sankey — mode shift or tour-to-trip mode consistency.

```yaml
- type:    sankey
  title:   Tour Mode to Trip Mode Consistency
  metric:  tour_mode_to_trip_mode
  height:  400
  source:  tour_mode
  target:  trip_mode
  value:   trips
  color_scheme: Tableau10
```

### `type: graphic-walker`

Graphic Walker — full-tab free-form Tableau-style sandbox. Explore tab only.
Snapshot model: does not share DuckDB connection or respond to global sidebar filters.

```yaml
- type:     graphic-walker
  title:    Free-form Visual Analytics
  dataset:  trip_mode_share     # which scenario__* view to query — also
                                 # the picker's initial selection when
                                 # dataset_picker is true (still required
                                 # either way)
  scenario: good_scenario       # optional — pins to one scenario's view,
                                 # same meaning as every other data-bound
                                 # panel type's own scenario: key; omit to
                                 # union across all currently-active
                                 # scenarios (the existing $scenario.
                                 # mechanism, adding a scenario column the
                                 # viewer can drag onto any shelf themselves)
  dataset_picker: true          # optional, default false
                                 # (028-graphic-walker-dataset-picker) —
                                 # shows the viewer a control listing every
                                 # dataset queryable against the active
                                 # scenario(s) (or, with scenario: also
                                 # set, against that one scenario alone)
                                 # and lets them switch it at view time.
                                 # Switching re-queries and discards any
                                 # chart the viewer had built. Absent or
                                 # false: identical to this panel type's
                                 # original fixed-dataset behavior.
  limit:    100000               # row limit passed to DuckDB before handoff
  height:   700
  width:    1.0
  fields:                       # optional — overrides the auto-inferred
                                 # field schema for specific columns, by fid
    - fid:          taz_id
      semanticType:  nominal    # quantitative | nominal | ordinal | temporal
      analyticType:  dimension  # dimension | measure
```

Column semantic/analytic types are inferred automatically from each
column's underlying data type (numeric → quantitative/measure, text →
nominal/dimension, date/timestamp → temporal/dimension) — a dashboard
author never has to hand-author a field-type definition for every column
of a dataset. `fields:` only needs an entry for the columns where that
default guess is wrong (014-graphic-walker-panel).

`dataset_picker`'s own list of selectable datasets only ever offers a
metric name that (a) has a real `{scenario}__{metric}` view for every
scenario in scope, and (b) has the SAME real columns in every one of
those views — a metric present everywhere but with genuinely different
columns per scenario (a data-authoring inconsistency, not something a
real `summarize.yaml` should ever produce, since one metric definition is
applied identically to every scenario) is deliberately excluded rather
than offered as a choice that would fail the moment it's picked
(028-graphic-walker-dataset-picker).

### `type: markdown`

Free-form text panel for notes, methodology descriptions, or section dividers.

```yaml
- type:    markdown
  content: |
    ## Highway Assignment Validation

    Screenline volumes compared against **UDOT AADT counts** (2019 base year).
    Acceptable error bands: ±10% for facilities > 10,000 AADT.
```

---

## Multi-scenario comparison

### Overlaid series (one panel, N scenarios)

Omit the `scenario:` key. The SQL expander generates a UNION ALL across all loaded
scenario views, adding a `scenario` column. The panel colors by scenario.

```sql
-- Generated automatically for metric: trip_mode_share, filter: purpose = HBW
SELECT 'abm_2026' AS scenario, mode, share
  FROM abm_2026__trip_mode_share WHERE purpose = 'HBW'
UNION ALL
SELECT 'base_tbm', mode, share
  FROM base_tbm__trip_mode_share WHERE purpose = 'HBW'
UNION ALL
SELECT 'Observed', mode, share
  FROM observed_mode_share WHERE purpose = 'HBW'
```

### Side-by-side panels

Add `scenario:` key to each panel. Each panel queries only its own view.

```yaml
row_compare:
  - type:     plotly
    title:    Base TBM — Mode Share
    metric:   trip_mode_share
    scenario: base_tbm
    traces:
      - type: bar
        x: $metric.purpose
        y: $metric.share
        color: $metric.mode
    width: 0.5

  - type:     plotly
    title:    ABM 2026 — Mode Share
    metric:   trip_mode_share
    scenario: abm_2026
    traces:
      - type: bar
        x: $metric.purpose
        y: $metric.share
        color: $metric.mode
    width: 0.5
```

---

## `manifest.yaml`

Auto-generated by the post-processor. Placed in the scenario folder root.
The `observed` manifest is hand-authored and committed to the dashboard repo.

```yaml
scenario_name:  abm_2026_baseline   # used as DuckDB view prefix: abm_2026_baseline__*
display_name:   2026 ABM Baseline   # shown in UI (falls back to scenario_name)
engine:         activitysim
model_version:  "1.3"
run_date:       2026-06-15
color:          "#4e79a7"           # auto-assigned from Tableau10 palette if omitted
notes:          First full-region ABM run post-calibration
pinned:         false               # observed/reference entry marker — excluded
                                    # from the automatic $baseline pick and shown
                                    # first. Does NOT control activation: every
                                    # `status: ready` scenario is active by default
                                    # (038-all-loaded-scenarios).
```

**Observed manifest** (`public/observed/manifest.yaml`):
```yaml
scenario_name:  observed
display_name:   Observed Data
color:          "#666666"
notes:          UDOT AADT counts, Utah Household Travel Survey, NHTS 2017
pinned:         true                # observed/reference marker (excluded from the
                                    # automatic $baseline, shown first). Active by
                                    # default only when its data loads (status:
                                    # ready) — an empty public/observed/ registers
                                    # `failed` and is not activated.
```

---

## Observed data

Observed data lives in `public/observed/summary/` — a scenario folder like any other,
but with `pinned: true` in its `manifest.yaml`. It is registered at startup as
`observed__*` views and, like every scenario whose data loads successfully, is
active by default (`038-all-loaded-scenarios`). Analysts can deselect it like any
other scenario. If `public/observed/` is empty (the real-deployment default — it is
gitignored), `observed` registers `failed` and is simply not part of any panel's
scenario union.

```
public/observed/
  manifest.yaml                  # pinned: true, name: "Observed Data"
  summary/
    summary_kpis.parquet         # scalar reference values
    observed_mode_share.parquet  # household travel survey mode shares
    observed_counts.parquet      # UDOT AADT screenline counts
    observed_tlfd.parquet        # survey-based trip length distribution
    observed_trip_rates.parquet  # trip generation rates by purpose
```

Reference in any panel — observed is just another scenario named `observed`:
```yaml
observed:
  source: observed__mode_share   # view name: observed__ prefix
  x:      purpose
  y:      share
  style:  dot                    # dot | bar | line
  name:   Observed
```

---

## Geography segmentation

Four levels, all available as columns in `trips_merged`, `tours_merged`,
`persons_merged`, and `households_merged` via the `JOIN zones z` fragment.
Sourced from `zones.parquet` (keyed to `taz_id`).

| Column | Level |
|---|---|
| `z.taz_id` | TAZ (finest grain — use for zone maps only) |
| `z.small_district` | Small district |
| `z.medium_district` | Medium district |
| `z.large_district` | Large district |
| `z.super_district` | Super district = county (use for tabular segmentation) |

Default for calibration summaries: `z.super_district`. TAZ reserved for
`zonemap` panels (choropleth maps).

---

## SQL placeholder reference

| Placeholder | Where used | Expands to |
|---|---|---|
| `$mappings.x` | metric SQL in summarize.yaml | WHEN clauses inside a CASE block |
| `$bins.x` | metric SQL in summarize.yaml | Full CASE expression |
| `$sql.x` | metric SQL in summarize.yaml | Inline SQL fragment |
| `$filters.x` | dashboard panel `filter:` | Current sidebar filter value |
| `$inputs.x` | dashboard panel `filter:` | Current panel-level input value |
| `$scenario.x` | dashboard panel traces | Auto-generates UNION ALL per scenario |
| `$baseline.x` | expander mechanism only — see note below | Single bare view reference to whichever scenario is currently marked baseline (018-baseline-scenario-designation) — `"{scenario}__{metric}"`, not a UNION ALL like `$scenario.x` |
| `$metric.col` | dashboard panel trace axes | Column reference in result set |

**`$baseline.x` (this table's own placeholder-expansion mechanism, `services/sqlExpander.ts`)
is still not reachable from any panel's `filter:`/SQL** — no
`dashboard-*.yaml` key routes a value into `expand()`'s sixth parameter yet
(018-baseline-scenario-designation's own explicit scope boundary, spec.md
FR-011, unchanged by `019`). **Do not confuse this with the separate `$baseline`
sentinel VALUE** a `comparison: diff`'s `a`/`b` field can hold (`019-baseline-diff-consumption`,
"Scenario comparison / diff mode" under `type: zonemap` above) — that
mechanism resolves the same underlying baseline designation
(`appState.getBaseline()`), but through `panels/panelQuery.ts`'s own
`resolveComparisonScenarioName()`, never through this table's placeholder-
regex mechanism at all. Both exist and both work; they are two independent
paths to the same piece of state, not one feature building on the other.

**`$filters.x` and the `all` sentinel:** when a filter's current value is the
`all` sentinel (see `all_option: true` above), the entire condition
referencing `$filters.x` is omitted rather than expanding into a condition
that matches nothing. This works by dropping the whole *line* the
placeholder appears on — so **`$filters.x` must sit alone on its own line**
in the metric SQL for omission to work correctly:

```sql
WHERE 1=1
  AND purpose = '$filters.purpose'   -- OK: this whole line is dropped when purpose = 'all'
```

Putting two conditions on one line with a `$filters.x` reference (e.g.
`AND purpose = '$filters.purpose' AND mode = 'SOV'`) drops **both**
conditions when the filter is `all`, not just the one referencing the
placeholder — write each filter-gated condition on its own line.

---

## Grammar design decisions

| Decision | Choice | Reason |
|---|---|---|
| Expression language | DuckDB SQL | Same in Python + WASM; one query language throughout |
| Mappings / binning | YAML → SQL CASE expansion | Readable, version-controllable, no pandas |
| Join logic | `sql_fragments` in summarize.yaml | Replaces separate preprocessor — join + aggregate = one SQL statement |
| Three config files only | summarize.yaml, dashboard-*.yaml, manifest.yaml | No topsheet.yaml (first dashboard = landing page), no dashboard-config.yaml |
| First dashboard = landing page | dashboard-1-summary.yaml | Eliminates redundant topsheet file type |
| File format | Parquet / GeoParquet throughout | No CSV, GeoJSON, or OMX in browser |
| Charting | Plotly default, Observable Plot for reactive inputs | Each excels at different things — see SPEC.md |
| Scenario comparison | Both overlaid and side-by-side supported | `scenario:` key controls which; omit for overlaid |
| Observed data | Permanent registered views, no prefix | Always available, no per-panel loading config |
| Geography | Four levels in zones.parquet; super_district default | TAZ only for map panels |
